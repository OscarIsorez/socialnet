import 'package:flutter/material.dart';

/// Central colour palette for the Redemton application.
///
/// Keep this file focused on foundational, brand-defining colours.
/// Feature-specific colour tweaks should be derived from these base values
/// to ensure a consistent look and feel throughout the product.
class AppColors {
  AppColors._();

  // static const Color primary = Color(0xFF6366F1);

  static const Color primary = Color(0xFFDAA06D);
  static const Color secondary = Color(0xFF22D3EE);
  // static const Color accent = Color(0xFFF97316);
  static const Color accent = Color(0xFF8B5CF6);

  static const Color scaffoldBackground = Color(0xFFF8FAFC);
  static const Color cardBackground = Colors.white;
  static const Color error = Color(0xFFEF4444);
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFFACC15);

  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF475569);
  static const Color border = Color(0xFFE2E8F0);
}
